version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: neutronx
      POSTGRES_PASSWORD: neutronx
      POSTGRES_DB: neutronx
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: