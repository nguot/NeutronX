% Cấu hình hiển thị mã nguồn (package listings đã được nạp trong DoAn.tex).
% Dùng kiểu đơn giản, không màu, để chắc chắn biên dịch được trên mọi máy.
\lstdefinelanguage{Solidity}{
  morekeywords={contract,function,require,return,returns,external,public,internal,private,
    view,pure,payable,memory,storage,calldata,address,uint256,uint160,uint128,uint64,uint32,
    uint16,uint8,bool,bytes32,bytes,mapping,struct,if,else,for,while,emit,immutable,constant,
    modifier,new},
  morecomment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]",
  sensitive=true
}
% Dùng \tcode{} thay cho \texttt{} với tên hàm test dài -- cho phép xuống dòng tại dấu gạch dưới.
\newcommand{\tcode}[1]{{\small\nolinkurl{#1}}}

\lstset{
  language=Solidity,
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\bfseries,
  commentstyle=\itshape,
  numbers=left,
  numberstyle=\tiny,
  numbersep=8pt,
  breaklines=true,
  frame=single,
  showstringspaces=false,
  tabsize=2,
  captionpos=b,
  xleftmargin=14pt,
}
